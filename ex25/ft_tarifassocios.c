void	ft_tarifassocios(int *tab, int size, void (*f)(int))
{
	while (tab[size])
	{
		ft_cambio(tab[size]);
		size++;
	}
}

ft_tarifa(
if (tab[size] > 13 && tab[size] < 16)
{
	return (10);
}
if (tab[size] > 16)
	return (20);
	
